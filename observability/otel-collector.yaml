# OpenTelemetry Collector configuration for NeMo Lens
#
# Handles the metrics and logs pipelines.  Traces go directly to Jaeger v2
# (which natively accepts OTLP).
#
# Pipelines:
#   Megatron (OTLP metrics) ──► Collector ──► Prometheus scrape endpoint
#   Megatron (OTLP logs)   ──► Collector ──► Elasticsearch

receivers:
  otlp:
    protocols:
      grpc:
        endpoint: 0.0.0.0:4317
      http:
        endpoint: 0.0.0.0:4318

processors:
  # Cap memory usage; drops data rather than OOM-crashing.
  memory_limiter:
    check_interval: 1s
    limit_mib: 512
    spike_limit_mib: 128

  # Batch before exporting — improves throughput.
  batch:
    send_batch_size: 512
    timeout: 5s

  # Attach host-level resource attributes (hostname, OS, etc.).
  resourcedetection:
    detectors: [env, system]
    timeout: 5s
    override: false

exporters:
  # Expose metrics as a Prometheus scrape endpoint (Prometheus polls this).
  prometheus:
    endpoint: "0.0.0.0:8889"
    send_timestamps: true
    # After a training job ends, keep last-seen metric values for 3 h
    # so Grafana doesn't show gaps between runs.
    metric_expiration: 3h
    # Promote resource attributes (service.name, host.name, …) to labels.
    resource_to_telemetry_conversion:
      enabled: true

  # Export logs to Elasticsearch.
  elasticsearch:
    endpoints: ["http://elasticsearch:9200"]
    logs_index: nemo-logs
    sending_queue:
      enabled: true
      num_consumers: 2
      queue_size: 1000

  # Debug: uncomment to log every received data point to collector stdout.
  # debug:
  #   verbosity: detailed

extensions:
  # Health-check endpoint — used by docker-compose healthcheck.
  health_check:
    endpoint: 0.0.0.0:13133

service:
  extensions: [health_check]

  telemetry:
    logs:
      level: warn

  pipelines:
    metrics:
      receivers:  [otlp]
      processors: [memory_limiter, resourcedetection, batch]
      exporters:  [prometheus]

    logs:
      receivers:  [otlp]
      processors: [memory_limiter, batch]
      exporters:  [elasticsearch]
